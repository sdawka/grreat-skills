For substantial tracked work, use the `grreat` skill. Start with this project's GRREAT manifest, normally `docs/grreat/README.md`, and read only its selected records. Plan mode develops Goals, Research, and Roadmap; execution records work, results, and time in the same linked Markdown vault. Use `rg` for exact lookup and an available project-scoped zvec-grep index for semantic discovery, verifying results against current source. Keep app sync optional and explicitly authorized; Markdown stays authoritative and conflicts remain visible.
